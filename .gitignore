# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Generated data / run artifacts (never commit prepared datasets or checkpoints)
outputs/
*.jsonl

# Gold strategy *images* are committed so Colab clones work without a rebuild.
# (JSON is always committed; prepared train/val datasets stay local/Drive-only.)

# Colab / Drive mounts, large model files
*.gguf
*.safetensors
*.bin

# OS / editor
.DS_Store
.vscode/
